# K8s binaries needed on worker nodes
k8s_binary_list: "{{ k8s_binaries.worker }}"

# PKI Certificates to transfer to worker nodes
certs_to_transfer:
  - "{{ certificate_authorities.kubernetes_ca.name }}"
  - "{{ inventory_hostname }}-key.pem"
  - "{{ inventory_hostname }}.pem"
