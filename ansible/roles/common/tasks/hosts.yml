- name: Update /etc/hosts with workers inventory hosts
  ansible.builtin.blockinfile:
    path: /etc/hosts
    block: |
      {% for host in groups['k8s_cluster'] %}
      {{ hostvars[host]['ansible_host'] }} {{ host }}.kubernetes.local {{ host }} 
      {% endfor %}
